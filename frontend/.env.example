# Para Android/iOS físico, use o IP local do computador que executa o backend.
# Não use localhost: no celular, localhost aponta para o próprio aparelho.
EXPO_PUBLIC_LK_CLIP_API_URL=http://192.168.1.100:8000
